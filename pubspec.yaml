name: chatgpt_flutter_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http_parser: ^4.0.2
  dart_openai: 
    path: plugins/dart_openai-2.0.1
  flutter_highlighting: ^0.9.0+11.8.0
  markdown: ^7.1.0
  flutter_markdown: ^0.6.18
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.2.1
  # http: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
flutter:
  uses-material-design: true
